//this function should be recursive

import React from "react";
import { ComponentFactory } from "./ComponentFactory";

export const Panel = (props) => {
    
    return (
        <div className="panel">{props}</div>
    )
};
